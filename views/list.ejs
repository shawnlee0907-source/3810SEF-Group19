<!DOCTYPE html>
<html>
<head>
  <title>Flight Dashboard</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet">
  <link href="/style.css" rel="stylesheet">
</head>
<body class="bg-light">
  <% if (success) { %>
    <div class="flash-message">
      <div class="alert alert-success alert-dismissible fade show">
        Success <%= success %>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
    </div>
  <% } %>

  <div class="dashboard-header">
    <div class="container">
      <div class="row align-items-center">
        <div class="col">
          <h1>Flight Dashboard</h1>
          <p class="lead mb-0">Welcome, <strong><%= user.name || user.id %></strong> (ID: <%= user.id %>) | <a href="/logout" class="text-white">Logout</a></p>
        </div>
        <div class="col-auto">
          <button class="btn btn-light btn-lg" data-bs-toggle="modal" data-bs-target="#addModal">
            Add Flight
          </button>
        </div>
      </div>
    </div>
  </div>

  <div class="container mt-4">
    <div class="row">
      <!-- 關鍵修正：唔再用 flights.length === 0，而係用 !flights || flights.length === 0 -->
      <% if (!flights || flights.length === 0) { %>
        <div class="col-12 text-center py-5">
          <h4 class="text-muted">No flights yet</h4>
          <p class="text-muted">Add your first flight using the button above!</p>
        </div>
      <% } else { %>
        <% flights.forEach(flight => { %>
          <div class="col-md-6 col-lg-4 mb-4">
            <div class="card flight-card h-100 shadow-sm">
              <div class="card-body">
                <div class="d-flex justify-content-between align-items-start mb-3">
                  <h5 class="card-title mb-0"><%= flight.flightNumber || 'N/A' %></h5>
                  <span class="badge status-<%= (flight.status || 'On Time').replace(' ', '') %>">
                    <%= flight.status || 'On Time' %>
                  </span>
                </div>
                <h6 class="card-subtitle mb-2 text-muted"><%= flight.airline || 'Unknown Airline' %></h6>
                
                <div class="flight-route my-3 fs-5 fw-bold">
                  <%= flight.departureAirport || 'HKG' %> to <%= flight.arrivalAirport || flight.destination || 'N/A' %>
                </div>
                
                <p class="card-text fw-bold text-primary mb-1">
                  Time: <%= String(flight.hours || '00').padStart(2,'0') %>:<%= String(flight.minutes || '00').padStart(2,'0') %>
                </p>

                <% if (flight.photo) { %>
                  <img src="data:image/png;base64,<%= flight.photo %>" class="img-fluid rounded mt-2" style="max-height: 120px;">
                <% } %>
              </div>
              
              <div class="card-footer bg-transparent border-0">
                <!-- 關鍵修正：用 id= 而唔係 _id=（同你 server.js 嘅 /details route 匹配）-->
                <a href="/details?id=<%= flight._id %>" class="btn btn-sm btn-outline-primary me-2">Details</a>
                <a href="/edit?_id=<%= flight._id %>" class="btn btn-sm btn-outline-warning me-2">Edit</a>
                <form action="/flights/<%= flight.flightNumber %>?_method=DELETE" method="POST" class="d-inline">
                  <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Delete <%= flight.flightNumber %>?')">
                    Delete
                  </button>
                </form>
              </div>
            </div>
          </div>
        <% }) %>
      <% } %>
    </div>
  </div>

  <!-- Add Flight Modal（你原本嗰個好靚，保留晒）-->
  <div class="modal fade" id="addModal" tabindex="-1">
    <!-- 你原本嘅 modal 內容全部保留 -->
    <!-- （為咗唔洗貼太長，我略咗，但你直接 copy 你原本嗰個就得）-->
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <script>
    setTimeout(() => document.querySelectorAll('.alert').forEach(a => a.remove()), 5000);
  </script>
</body>
</html>
